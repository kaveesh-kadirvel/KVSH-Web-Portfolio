html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KVSH Portfolio</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div id="main">
        <nav>
            <div class="logo">KVSH</div>
            <ul class="nav-links">
                <li><a href="#about">About</a></li>
                <li><a href="#experience">Experience</a></li>
                <li><a href="#projects">Projects</a></li>
                <li><a href="#skills">Skills</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>

        <div class="page">
            <div class="hero-text">
                <h1>Web Developer</h1>
            </div>
            <div class="hero"></div>
        </div>

        <div class="page1">
            <div class="aboutme-image"></div>
            <div class="aboutme-note">
                <h1>About Me</h1>
                <br>
                <p></p> Hi! I’m Kaveesh Kadirvel, a second-year undergraduate student at SIES
 Graduate School of Technology, pursuing a B.E. in Information Technology.
 <br>
 <br>
 I’m passionate about web development and have been learning and building projects since my first year of college. Over time, I’ve created several web-based applications that showcase my skills in both frontend and backend development.
 <br>
 <br>
I enjoy transforming ideas into interactive, user-friendly digital experiences whether working independently or as part of a team. Building web projects isn’t just a skill for me it’s something I truly enjoy and take pride in.</p>
            </div>
        </div>
    </div>
    <script src="script.js"></script>
</body>
</html> 


css
*{
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  height: 100%;
  width: 100%;
}

.main{
    position: relative;
    overflow: hidden;
}

nav{
  padding: 1.5% 2%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
nav .logo{
  font-size: 1.5rem;
  font-weight: bold;
  font-family:'Times New Roman', Times, serif;
}
nav ul{
  list-style: none;
  display: flex;
  gap: 2rem;
}
nav ul li a{
  text-decoration: none;
  font-size: 1.2rem;
  color: black;
  font-family:'Times New Roman', Times, serif;
}


.page {
    height: 100vh;
    width: 100%;
    background-image: url("background image.png");
    background-size: cover;
    background-position: center;
    align-items: center;
    justify-content: center;
    display: flex;
}

.hero-text {
  position: absolute;
  top: 15%;
  font-size: 4rem;
  color: black;
  text-align: center;
  font-family: 'Times New Roman', Times, serif;
}

.hero {
  height: 20rem;
  width: 20rem;
  background-image: url("kaveesh headshot.png");
  background-size: cover;
  background-position: center;
  border-radius: 50%;
  z-index: 1000;

  /* ➜ Required for hover smoothness */
  transition: transform 0.35s ease, box-shadow 0.35s ease;
  cursor: pointer;
}

/* ➜ Hover effect (zoom + shadow) */
.hero:hover {
  transform: scale(1.06);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.35);
}

/*about page styles*/
.page1{
    height: 100vh;
    width: 100%;
    background-color:black;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-family:'Times New Roman', Times, serif;
    color: white;
    padding-left: 5%;
}

.aboutme-image {
  height: 30rem;
  width: 30rem;
  background-image: url("kaveesh headshot.png");
  background-size: cover;
  background-position: center;
  border-radius: 5%;
  transition: transform 0.35s ease, box-shadow 0.35s ease;
  cursor: pointer;
}

.aboutme-image:hover {
  transform: scale(1.05);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
}


.aboutme-note{
  width: 50%;
  padding-right: 5%;
  font-size: 1.2rem;
  line-height: 1.5;
}

.aboutme-note h1{
  text-align: center;
  font-size: 2.5rem;
  margin-bottom: 1rem;
}

js
// Text phrases
const phrases = ['Web Developer', 'Kaveesh Kadirvel'];

// Typing speeds
const typingSpeed = 80;       // ms per character
const deletingSpeed = 40;     // ms per character
const pauseAfterTyped = 1000; // ms after finishing typing
const pauseAfterDeleted = 300; // ms after deleting

const el = document.getElementById('typewriter');

// Helper: delay
const wait = (ms) => new Promise(res => setTimeout(res, ms));

async function typeText(text) {
  for (let i = 0; i <= text.length; i++) {
    el.textContent = text.slice(0, i);
    await wait(typingSpeed);
  }
}

async function deleteText(text) {
  for (let i = text.length; i >= 0; i--) {
    el.textContent = text.slice(0, i);
    await wait(deletingSpeed);
  }
}

async function runLoop() {
  let i = 0;
  while (true) {
    const text = phrases[i % phrases.length];
    await typeText(text);
    await wait(pauseAfterTyped);
    await deleteText(text);
    await wait(pauseAfterDeleted);
    i++;
  }
}

// Start after DOM loads
if (document.readyState === "loading") {
  document.addEventListener("DOMContentLoaded", runLoop);
} else {
  runLoop();
}
